# KPL Animated GitHub Profile — @adityayedpalwar

1. Create a **public** repository named exactly `adityayedpalwar`.
2. Upload this folder's contents.
3. Keep `README.md` at the root.
4. Go to **Actions → Update profile art → Run workflow** once.
5. The scheduled workflow refreshes the contribution SVG daily.

The fetcher uses GitHub's public contribution calendar and does not require a personal access token.
